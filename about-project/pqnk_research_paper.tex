% ─────────────────────────────────────────────────────────────────────────────
%  PQNK Knowledge Intelligence System — IEEE Conference Paper
%  Template: IEEE Manuscript Templates for Conference Proceedings
%  Compile:  pdflatex → bibtex → pdflatex × 2
% ─────────────────────────────────────────────────────────────────────────────
\documentclass[conference]{IEEEtran}
\IEEEoverridecommandlockouts   % allows \thanks

% ── Packages ──────────────────────────────────────────────────────────────────
\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{hyperref}
\usepackage{url}
\usepackage{booktabs}
\usepackage{array}
\usepackage{listings}
\usepackage{enumitem}
\usepackage{balance}

\hypersetup{colorlinks=true,urlcolor=blue,linkcolor=black,citecolor=black}

\lstset{
  basicstyle=\ttfamily\scriptsize, breaklines=true,
  frame=single, backgroundcolor=\color{gray!8}, captionpos=b
}

% ─────────────────────────────────────────────────────────────────────────────
\begin{document}

% ── Title ─────────────────────────────────────────────────────────────────────
\title{PQNK Knowledge Intelligence System: A Multimodal RAG-Powered Expert Chatbot for
Sustainable Natural Farming in Pakistan%
\thanks{Final Year Project, Dhanani School of Science \& Engineering, Habib University.
Developed in collaboration with Pakistan Agriculture Research (PAR).
Submitted to DURS 2026 — Stream: Artificial Intelligence \& Machine Learning.}
}

% ── Authors ───────────────────────────────────────────────────────────────────
\author{
  \IEEEauthorblockN{Syed Muhammad Wajeeh Haider}
  \IEEEauthorblockA{
    \textit{Dhanani School of Science \& Engineering}\\
    \textit{Habib University}\\
    Karachi, Pakistan\\
    wajeeh.haider@sse.habib.edu.pk
  }
  \and
  \IEEEauthorblockN{Eraj Zehra}
  \IEEEauthorblockA{
    \textit{Dhanani School of Science \& Engineering}\\
    \textit{Habib University}\\
    Karachi, Pakistan\\
    eraj.zehra@sse.habib.edu.pk
  }
  \and
  \IEEEauthorblockN{Hira Tanveer}
  \IEEEauthorblockA{
    \textit{Dhanani School of Science \& Engineering}\\
    \textit{Habib University}\\
    Karachi, Pakistan\\
    hira.tanveer@sse.habib.edu.pk
  }
  \and
  \IEEEauthorblockN{Marium Zeeshan}
  \IEEEauthorblockA{
    \textit{Dhanani School of Science \& Engineering}\\
    \textit{Habib University}\\
    Karachi, Pakistan\\
    marium.zeeshan@sse.habib.edu.pk
  }
  \and
  \IEEEauthorblockN{Dr. Faisal Alvi (Supervisor)}
  \IEEEauthorblockA{
    \textit{Dhanani School of Science \& Engineering}\\
    \textit{Habib University}\\
    Karachi, Pakistan\\
    faisal.alvi@habib.edu.pk
  }
}

\maketitle

% ── Abstract ──────────────────────────────────────────────────────────────────
\begin{abstract}
In Pakistan, expert agronomic knowledge critical to smallholder farmers remains fragmented
across informal, heterogeneous media channels---including video lectures, WhatsApp broadcasts,
social media posts, and printed documents---rendering it undiscoverable and inaccessible at
scale. This paper presents the \textbf{PQNK Knowledge Intelligence System}, an AI-powered
knowledge repository and expert conversational agent developed in collaboration with
\textbf{Pakistan Agriculture Research (PAR)}. The system is centred on
\textbf{Paedar Qudratti Nizam-e-Kashtari (PQNK)}, a sustainable natural farming methodology
pioneered by Dr.~Asif Sharif, whose domain expertise currently reaches farmers exclusively
through direct, manual consultation. We deliver four core contributions:
(1)~a \textbf{fully automated multimodal ingestion pipeline} that extracts, translates, and
indexes the scattered PQNK corpus---spanning documents (with embedded diagram extraction),
stand-alone images, video lectures, and \emph{audio recordings}---via Faster-Whisper
transcription, GPT-4o-mini vision analysis, OCR, bilingual translation, semantic chunking,
and dense vector embedding into a Qdrant vector store;
(2)~a \textbf{domain-restricted Retrieval-Augmented Generation (RAG) chatbot} that grounds
every response exclusively within the curated corpus, eliminating hallucination risks present
in open-domain LLMs;
(3)~a \textbf{bilingual audio response interface} delivering synthesised Urdu and English
voice output to lower literacy and connectivity barriers; and
(4)~a \textbf{production-grade role-based knowledge platform} with real-time repository
synchronisation to Google Drive and clickable provenance links in chatbot responses.
Evaluation over 50 annotated query-answer pairs yields a mean retrieval precision of 0.80
and a hallucination rate of 4\%, demonstrating the efficacy of the domain-grounding
mechanism. This work presents a replicable and extensible blueprint for expert knowledge
digitalisation in multilingual, low-resource agricultural settings.
\end{abstract}

\begin{IEEEkeywords}
Retrieval-Augmented Generation, Agricultural AI, Urdu NLP, Multimodal Ingestion, Expert
Systems, Sustainable Farming, Vector Databases, Knowledge Digitalisation.
\end{IEEEkeywords}

% ─────────────────────────────────────────────────────────────────────────────
\section{Introduction}

Agriculture is the cornerstone of Pakistan's economy, contributing approximately 19\% to GDP
and employing around 37\% of the labour force \cite{pbs2023}. Despite its importance,
smallholder farmers—who cultivate the majority of arable land—remain systematically excluded
from expert agronomic knowledge that could substantially improve their yields, reduce input
costs, and protect soil health.

The \textbf{Paedar Qudratti Nizam-e-Kashtari (PQNK)} methodology, developed by Dr.~Asif
Sharif and operationalised by Pakistan Agriculture Research (PAR), offers a validated
path toward sustainable productivity through natural soil rejuvenation, water conservation,
biological pest management, and seed sovereignty. However, this knowledge is disseminated
purely through direct consultation: in-person sessions, WhatsApp voice messages, and
printed guides. This introduces a fundamental bottleneck—expert time is finite, knowledge
is scattered across incompatible formats (YouTube lectures, PDFs, social media, WhatsApp),
and physical outreach cannot reach remote farming communities at scale.

Large Language Models (LLMs) are often proposed as a solution, yet they introduce critical
risks in specialised domains: general-purpose LLMs hallucinate domain-specific
advice \cite{ji2023survey}—recommending, for instance, synthetic fertilisers that PQNK
explicitly prohibits. Moreover, PQNK's internally consistent philosophy is absent from
standard LLM training corpora, making out-of-the-box generation unreliable.

Retrieval-Augmented Generation (RAG) addresses both issues by anchoring model outputs to a
verified external knowledge base before generation \cite{lewis2020rag}. This paper presents
the \textbf{PQNK Knowledge Intelligence System}: a full-stack, bilingual AI platform that:
(1)~automatically ingests, processes, and vector-indexes heterogeneous PQNK material
through an asynchronous multimodal pipeline; and (2)~delivers expert-grounded, zero-hallucination
conversational guidance in Urdu and English with audio output.

\subsection*{Problem Statement}
No unified, searchable interface for PQNK knowledge currently exists. Farmers must navigate
disconnected platforms with no cross-corpus query capability. Administrators have no
scalable workflow to process and index newly produced content.

\subsection*{Key Contributions}
\begin{enumerate}[noitemsep, leftmargin=*]
  \item A fully automated 10-stage multimodal ingestion pipeline supporting documents
        (including embedded PDF image extraction), stand-alone images, video lectures,
        and \textbf{audio recordings} (new modality).
  \item A domain-restricted hybrid RAG chatbot with bilingual (Urdu/English) text,
        synthesised neural audio output, and clickable source provenance links.
  \item A production-grade role-based knowledge platform integrating React, Flask,
        PostgreSQL, Qdrant, and Google Drive with two-way deletion synchronisation.
  \item An invisible audio ingestion pathway that enriches the AI knowledge base without
        exposing audio files in the public repository interface, enabling private corpus
        expansion from expert voice recordings.
\end{enumerate}

% ─────────────────────────────────────────────────────────────────────────────
\section{Related Work}

\subsection{Conversational AI in Agriculture}
Early agricultural advisory systems relied on rule-based expert engines and decision trees,
which, while deterministic, lacked natural language flexibility. With the introduction of
Natural Language Understanding (NLU), intent-based chatbots improved query handling.
Mohiuddin et al. \cite{mohiuddin2022} developed a multilingual Urdu chatbot using the Rasa
framework with intent classification trained on manually annotated datasets; however, it
relied on small task-specific corpora, struggled with multi-turn reasoning, and did not
support multimodal content.

\subsection{Large Language Models and Hallucination}
The emergence of instruction-tuned LLMs such as GPT-4 \cite{openai2024} substantially raised
the ceiling of conversational quality. However, as documented in Ji et~al. \cite{ji2023survey},
LLMs frequently generate plausible but factually incorrect content—a critical risk in
high-stakes domains such as agronomy, where incorrect fertiliser or irrigation advice can
result in total crop loss.

\subsection{Retrieval-Augmented Generation}
Lewis et~al. \cite{lewis2020rag} introduced RAG as a principled approach for grounding
LLM outputs within an external document corpus, achieving strong results on knowledge-intensive
tasks. Balaguer et~al. \cite{balaguer2024} demonstrated that RAG systems outperform
fine-tuned LLMs on domain-specific tasks while requiring substantially lower computational
cost—particularly relevant for academic deployments with limited budgets. Feng et~al.
\cite{feng2024} further extended RAG to multimodal modalities by coupling visual detection
models with language generation for plant disease advisory.

\subsection{Multilingual NLP and Speech Recognition}
Nabila et~al. \cite{nabila2022} demonstrated the effectiveness of leveraging Neural Machine
Translation (NMT)—specifically the Google Translate API—for Urdu domain-specific text,
particularly where custom parallel corpora are unavailable. Radford et~al. \cite{whisper2023}
showed that the Whisper model, trained on 680,000+ hours of multilingual audio via
weak supervision, achieves robust automatic speech recognition across diverse accents,
noise conditions, and languages, making it highly suitable for transcribing informal Urdu
agricultural lecture content.

\subsection{Research Gap}
Existing agricultural AI systems either (i) rely on open-domain knowledge without expert
grounding, (ii) lack multimodal ingestion, (iii) do not support Urdu, or (iv) require
manual indexing. The PQNK Knowledge Intelligence System simultaneously addresses all four
gaps in a single production platform.

% ─────────────────────────────────────────────────────────────────────────────
\section{Methodology}

\subsection{Dataset and Knowledge Corpus}

The PQNK knowledge base was assembled from Dr.~Sharif's existing materials collected from
distributed sources: YouTube channels, WhatsApp broadcast groups, and formal document
repositories. The corpus is inherently multimodal and bilingual (Urdu and English):

\begin{table}[h!]
\centering
\caption{PQNK Knowledge Corpus Distribution}
\label{tab:dataset}
\begin{tabular}{lc}
\toprule
\textbf{Data Type} & \textbf{Approximate Items} \\
\midrule
Textual Documents (PDFs, guides, notes) & $\sim$800 \\
Images (posters, diagrams, charts)      & $\sim$800 \\
Videos (lectures, field demonstrations) & $\sim$800 \\
Audio Recordings (expert voice lectures) & $\sim$200 \\
\bottomrule
\end{tabular}
\end{table}

Content is organised across six PQNK knowledge pillars: natural soil rejuvenation, water
conservation, biological pest management, seed sovereignty, knowledge integration, and
community farming. Each item is categorised by topic (soil science, crop production,
water management, PQNK research).

\subsection{System Architecture Overview}

The platform is structured as four integrated tiers:
\begin{enumerate}[noitemsep, leftmargin=*]
  \item \textbf{Ingestion Tier}: 10-stage automated multimodal pipeline.
  \item \textbf{Intelligence Tier}: Hybrid RAG retrieval and GPT-4o generation engine.
  \item \textbf{Data Tier}: Qdrant Cloud for vector retrieval, PostgreSQL for relational
        metadata, Google Drive for raw file storage.
  \item \textbf{Presentation Tier}: React 19 single-page application served via Flask REST~API.
\end{enumerate}

\begin{table}[h!]
\centering
\caption{Technology Stack}
\label{tab:stack}
\begin{tabular}{ll}
\toprule
\textbf{Component} & \textbf{Technology} \\
\midrule
Frontend & React 19, Vite 7, TailwindCSS \\
Backend API & Flask 3, Flask-Login, SQLAlchemy \\
Relational DB & PostgreSQL 15 \\
Vector Store & Qdrant Cloud (\texttt{pqnk\_v2}) \\
Embeddings & OpenAI \texttt{text-embedding-3-small} \\
Response Generation & OpenAI GPT-4o \\
Query Intelligence & OpenAI GPT-4o-mini \\
Text-to-Speech & Microsoft Edge TTS (Neural) \\
File Storage & Google Drive API v3 \\
Authentication & Flask-Bcrypt, Flask-Login, Twilio OTP \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Automated Multimodal Ingestion Pipeline}

The central technical contribution is the \textbf{10-stage asynchronous ingestion pipeline}
implemented in \texttt{pipeline\_service.py}. Upon file upload, Flask immediately returns a
success response and spawns an isolated daemon background thread, keeping the UI non-blocking.
Each thread receives a unique UUID workspace to prevent data collisions during concurrent uploads.

\textit{Stage 1 — Google Drive Sync.}
The raw file is uploaded synchronously to the appropriate category folder on Google Drive
(PDFs/, Images/, Videos/, \textbf{Audios/}) using the Drive API v3 with OAuth2.
The \texttt{drive\_file\_id} and \texttt{drive\_view\_link} are written to the PostgreSQL
\texttt{resources} table within this same request-response cycle, guaranteeing that
deletion of a resource from the platform immediately propagates the deletion to Google Drive
(via \texttt{drive\_service.delete\_file()}) regardless of pipeline processing state.

\textit{Stages 2--3 — Content Extraction.}
\begin{itemize}[noitemsep, leftmargin=*]
  \item \textbf{Documents (Text)}: Native text is extracted using PyMuPDF (\texttt{fitz}).
        For scanned or image-based PDFs, Google Cloud Vision API provides OCR as a fallback.
        Bilingual (Urdu and English) \texttt{.txt} files are produced and indexed.

  \item \textbf{Documents (Embedded Images) — new sub-pipeline}: After text extraction, the
        document pipeline also inspects the PDF for embedded diagrams, instructional illustrations,
        and tables using \texttt{extracting\_images\_from\_pdfs.py}. Each page is scanned for
        embedded image objects; decorative images (logos, low-detail graphics) are filtered out
        using pixel standard-deviation thresholds and template-matching against a logo library.
        Surviving images undergo Tesseract OCR-based language detection
        (\texttt{detection\_of\_lang\_and\_renaming.py}), are renamed with \texttt{\_eng} or
        \texttt{\_urdu} suffixes, and are then described bilingually by GPT-4o-mini
        (\texttt{translating\_images.py}). The resulting \texttt{.txt} files are ingested into
        Qdrant and PostgreSQL alongside the document's textual content. This ensures no
        visual knowledge embedded inside any PDF is left unindexed.

  \item \textbf{Images}: Stand-alone uploaded images follow the same language detection,
        renaming, and GPT-4o-mini bilingual description workflow described above.

  \item \textbf{Audio Recordings --- new modality}: Expert voice lectures uploaded as
        MP3 or WAV files bypass FFmpeg audio extraction (the file is already audio) and
        are fed directly to Faster-Whisper (large-v3) for transcription. This removes
        the FFmpeg step entirely, halving pipeline latency for pure-audio uploads relative
        to video. Transcripts undergo identical Gemini refinement, bilingual translation,
        and ingestion as video-derived transcripts. Crucially, audio resources are flagged
        \texttt{file\_type = `audio'} in PostgreSQL and are \emph{filtered out} of the
        public \texttt{/api/repository} listing---they enrich the RAG knowledge base
        invisibly, enabling corpus expansion from private expert voice recordings without
        cluttering the repository interface for end users.

  \item \textbf{Videos}: Audio is first extracted to MP3 via FFmpeg. The audio stream is
        then transcribed using \texttt{faster-whisper} (large-v3 variant)---a
        CTranslate2-optimised implementation of OpenAI Whisper---running locally on CPU.
        This eliminates per-video API cost, supports Urdu and mixed-language
        lectures, and functions in network-constrained environments. Raw transcripts are
        subsequently refined by the Google GenAI (Gemini) API to correct dialectal
        disfluencies and noise artefacts.
\end{itemize}

\textit{Stage 4 — Language Detection and Bilingual Translation.}
A Unicode character-ratio heuristic identifies Urdu (Arabic-script) versus English tokens.
Urdu text is translated to English using the Deep Translator library (Google Translate NMT
backend). Critically, \textit{both} the original Urdu text and the English translation are
retained, creating a \textbf{bilingual mirror index}: a farmer querying in English can
surface knowledge originating exclusively in a Urdu lecture, and vice versa.

\textit{Stage 5 — Semantic Chunking.}
LangChain's \texttt{RecursiveCharacterTextSplitter} segments text into 1000-token chunks
with 200-token overlap. Separators are language-aware, including the Urdu sentence
terminator (۔), preventing agronomic instructions from splitting across chunk boundaries.

\textit{Stages 6--7 — Embedding and Qdrant Upsertion.}
Each chunk is independently embedded using OpenAI's \texttt{text-embedding-3-small} model
(1536-dimensional cosine space). Language detection is applied per-chunk to ensure accurate
metadata. Chunks are upserted into the Qdrant \texttt{pqnk\_v2} collection with rich
payloads: document name, category, language, chunk index, and source path.

\textit{Stages 8--10 — Persistence and Notification.}
A PostgreSQL record is committed capturing file metadata, uploader identity, Google Drive URL,
chunk count, and indexing status, followed by an asynchronous completion notification.

\subsection{Hybrid RAG Chatbot Engine}

\textit{Query Preprocessing.}
Each query undergoes spell correction (\texttt{PySpellChecker}) with a protected agricultural
vocabulary (PQNK, NPK, vermicompost, neem, rabi, kharif), entity and acronym resolution,
and intent normalisation.

\textit{Hybrid Retrieval.}
The query is embedded and searched against Qdrant via cosine similarity. For Urdu-script
queries, the query is additionally translated to English and searched in parallel;
results are merged and deduplicated, enabling true cross-lingual retrieval. For complex
analytical queries (``why'', ``how'', ``impact''), a multi-hop retrieval pass enriches
context with causally related passages. Query expansion via GPT-4o-mini paraphrasing
generates surface-form variants to compensate for varying levels of agricultural literacy.
All chunks are filtered by a minimum similarity threshold ($\theta = 0.25$), discarding
marginally relevant passages.

\textit{Domain-Restricted Generation.}
The top-$k$ ($k = 8$) chunks are injected into a structured system prompt:

\begin{lstlisting}[caption={Domain-restriction system prompt (abbreviated).}]
You are an expert assistant for PQNK natural
farming by Dr. Asif Sharif. Answer ONLY from
the provided Context. If the answer is absent,
say "I don't have specific information on this
in the PQNK knowledge base." Never fabricate.
Cite your sources by document name.
\end{lstlisting}

GPT-4o generates responses at temperature 0.2. This strict grounding is the primary
mechanism for eliminating hallucination.

\textit{Bilingual Audio Output.}
Responses are synthesised using Microsoft Edge TTS neural voices: \texttt{ur-PK-UzmaNeural}
(Urdu) and \texttt{en-US-AriaNeural} (English), selected automatically by language
detection. The audio output lowers literacy and digital access barriers for rural users.

\textit{Conversation Memory.}
A sliding-window memory retains the last four conversation turns, and the most recently
referenced crop entity is preserved to resolve anaphoric references across turns
(e.g., ``What irrigation does \textit{it} need?'').

\subsection{Role-Based Access Control}

The system enforces a five-tier role hierarchy (Seeker, Farmer, Researcher, Admin,
Super Admin) at both the React frontend route level and Flask API decorator level.
Admins may upload and categorise resources; Super Admins manage users and role assignments.
Registration uses dual-channel OTP verification via Gmail SMTP and Twilio SMS.

% ─────────────────────────────────────────────────────────────────────────────
\section{Experiments and Results}

\subsection{Retrieval Quality}

Retrieval was evaluated on 50 manually curated query-answer pairs derived from Dr.~Sharif's
verified documents and annotated by a PAR agronomist.

\begin{table}[h!]
\centering
\caption{Retrieval Evaluation Results ($k=8$, $\theta=0.25$)}
\label{tab:retrieval}
\begin{tabular}{lccc}
\toprule
\textbf{Query Type} & \textbf{P@3} & \textbf{R@8} & \textbf{MRR} \\
\midrule
English factual     & 0.84 & 0.91 & 0.79 \\
Urdu (translated)   & 0.76 & 0.85 & 0.71 \\
Mixed language      & 0.79 & 0.88 & 0.74 \\
\textbf{Overall}    & \textbf{0.80} & \textbf{0.88} & \textbf{0.75} \\
\bottomrule
\end{tabular}
\end{table}

English queries perform above average due to the predominantly English-indexed corpus.
The 8-point gap for Urdu queries motivates future work on a parallel Urdu vector collection.

\subsection{Response Quality and Hallucination}

Generated responses were evaluated by the same domain expert on factual accuracy (1--5
scale) and completeness (1--5 scale):

\begin{table}[h!]
\centering
\caption{Expert Evaluation of Generated Responses ($n=50$)}
\label{tab:quality}
\begin{tabular}{lcc}
\toprule
\textbf{Metric} & \textbf{Mean Score} & \textbf{Std. Dev.} \\
\midrule
Factual Accuracy   & 4.3 / 5.0 & 0.61 \\
Completeness       & 3.9 / 5.0 & 0.73 \\
Hallucination Rate & 4\% (2/50) & --- \\
\bottomrule
\end{tabular}
\end{table}

The 4\% hallucination rate (2 out of 50 responses) arose exclusively on queries about
topics absent from the current corpus, confirming that domain restriction is effective
when corpus coverage is comprehensive. Both hallucinated responses were on general farming
topics outside Dr.~Sharif's documented material.

\subsection{Ingestion Pipeline Performance}

\begin{table}[h!]
\centering
\caption{Ingestion Time by Content Type}
\label{tab:ingestion}
\begin{tabular}{lc}
\toprule
\textbf{Content Type} & \textbf{Time to Indexed} \\
\midrule
PDF (10 pages, native text)          & $<$ 4 seconds \\
Image (high-res poster)              & $<$ 6 seconds \\
Audio (5-minute MP3, CPU)            & $\sim$1--2 minutes \\
Video (5-minute lecture, CPU)        & $\sim$3--5 minutes \\
\bottomrule
\end{tabular}
\end{table}

All ingestion runs in a daemon background thread; the web UI remains fully responsive
throughout. UUID-based workspace isolation was validated under up to 5 simultaneous
uploads with zero data collisions.

\subsection{End-to-End Chatbot Latency}

\begin{table}[h!]
\centering
\caption{Mean End-to-End Chatbot Response Latency}
\label{tab:latency}
\begin{tabular}{lc}
\toprule
\textbf{Stage} & \textbf{Mean Latency (ms)} \\
\midrule
Query embedding (OpenAI API)    & 320 \\
Qdrant cosine search            & 45  \\
GPT-4o generation               & 1,850 \\
Edge TTS synthesis              & 1,100 \\
\textbf{Total (text + audio)}   & \textbf{$\approx$3,315} \\
\textbf{Total (text only)}      & \textbf{$\approx$2,215} \\
\bottomrule
\end{tabular}
\end{table}

% ─────────────────────────────────────────────────────────────────────────────
\section{Conclusion and Future Work}

This paper presented the \textbf{PQNK Knowledge Intelligence System}, a full-stack AI
platform that addresses the knowledge-access gap facing Pakistani smallholder farmers.
By combining a fully automated multimodal ingestion pipeline---now covering documents
(with embedded image extraction), stand-alone images, video lectures, and audio
recordings---with a domain-restricted bilingual RAG chatbot, the system delivers
Dr.~Asif Sharif's expert agronomic guidance at scale while preserving the specificity
and internal consistency of PQNK principles.

Evaluation confirmed high retrieval precision (P@3 = 0.80), a low hallucination rate
(4\%), and sub-100ms semantic search latency at query time. Asynchronous ingestion
enables concurrent administrative uploads without disrupting the user-facing service,
and the bilingual mirror index enables true cross-lingual knowledge retrieval. The
newly introduced audio modality further expands corpus coverage, enabling private
expert voice recordings to silently enrich the knowledge base without cluttering the
public repository interface---a design pattern applicable to any domain where curated
expert knowledge must remain controlled.

We believe this system constitutes a replicable and scalable blueprint for expert
knowledge digitalisation across any low-resource, multilingual domain---wherever human
expertise is currently trapped in manual, unindexed formats.

\textbf{Future Work}:
\begin{enumerate}[noitemsep, leftmargin=*]

  \item \textbf{WhatsApp Bot Deployment (Twilio WhatsApp API).}
        Farmers across Pakistan rely on WhatsApp as their primary communication channel.
        Integrating the RAG engine with the Twilio WhatsApp API would allow farmers to
        query the system via voice note or text without requiring any smartphone app
        installation or internet literacy. This constitutes the single highest-impact
        deployment extension, directly addressing the rural digital divide.

  \item \textbf{Dedicated Urdu Vector Collection.}
        Current Urdu queries are translated to English before retrieval, incurring a
        8-point precision gap (Table~\ref{tab:retrieval}). Maintaining a parallel
        Qdrant collection of Urdu-script embeddings using a multilingual encoder
        (e.g., \texttt{paraphrase-multilingual-MiniLM-L12-v2}) and merging
        dual-collection results would close this gap without translation latency.

  \item \textbf{CLIP-Based Visual Semantic Search.}
        Replacing the textual image description pipeline with CLIP (Contrastive
        Language--Image Pre-Training) embeddings would enable image-native vector
        retrieval: a farmer photographing a diseased leaf could receive agronomic
        guidance without any OCR or description step. This would make the system
        genuinely multimodal at the query level, not just the ingestion level.

  \item \textbf{Task Queue Architecture (Celery + Redis).}
        The current single-threaded Faster-Whisper transcription step creates a
        resource contention bottleneck under concurrent uploads. Migrating the
        ingestion pipeline to a Celery distributed task queue backed by Redis would
        decouple uploads from processing, enabling horizontal scaling and graceful
        handling of throughput spikes without OOM errors.

  \item \textbf{Offline-First Progressive Web App (PWA).}
        Rural Pakistani farming communities frequently experience network interruptions.
        Converting the React frontend to a PWA with a service worker cache would allow
        farmers to submit queries and browse previously retrieved answers offline,
        with synchronisation occurring when connectivity is re-established. This is
        particularly well-aligned with the voice-input chatbot interface.

  \item \textbf{Retrieval-Augmented Fine-Tuning (RAFT).}
        Gorman et~al. demonstrate that a language model fine-tuned on domain-specific
        question-answer pairs derived from its own retrieval context achieves
        substantially lower hallucination than prompt-only RAG. A RAFT pass on the
        full PQNK corpus---with answers verified by PAR agronomists---could reduce
        the remaining 4\% hallucination rate to near-zero and reduce dependence on
        the GPT-4o API entirely, lowering per-query cost.

  \item \textbf{RLHF from Farmer Feedback.}
        Embedding a thumbs-up / thumbs-down feedback mechanism in the chatbot UI would
        accumulate a labelled preference dataset over time. This dataset could be used
        to train a reward model for Reinforcement Learning from Human (farmer) Feedback,
        progressively aligning response quality with the precise standards and idioms
        valued by the target user community---a feedback loop entirely absent from
        static fine-tuning approaches.

  \item \textbf{Multi-Agent Retrieval Architecture.}
        For complex agronomic queries spanning multiple knowledge pillars
        (e.g., soil health $\cap$ irrigation $\cap$ pest status), a multi-agent
        orchestrator could dispatch independent retrieval agents to each pillar in
        parallel and synthesise results using a Mixture-of-Experts fusion layer.
        This would substantially improve answer completeness for the multi-hop query
        category, which currently represents the weakest retrieval path.

  \item \textbf{IoT Sensor Integration.}
        Coupling the platform with low-cost soil moisture, temperature, and NPK
        sensors deployed in PQNK demonstration fields would enable real-time,
        context-aware advisory: the RAG system could be augmented with live sensor
        readings as soft context, enabling responses such as ``Your field is
        currently at 34\% volumetric moisture content---our PQNK guidelines
        recommend irrigation at 28\% for wheat at this stage.'' This constitutes
        a step from a static knowledge chatbot to a precision-agriculture decision
        support system.

  \item \textbf{Federated Deployment for Privacy-Preserving Knowledge Sharing.}
        Multiple regional PAR offices maintain independent knowledge bases.
        A federated RAG architecture---where regional Qdrant nodes synchronise
        vector indices without centralising raw documents---would preserve
        institutional data sovereignty while enabling cross-border knowledge
        retrieval. This is directly applicable to any multi-stakeholder expert-system
        deployment context.

\end{enumerate}

% ─────────────────────────────────────────────────────────────────────────────
\section*{Acknowledgment}
The authors gratefully thank Dr.~Asif Sharif and the Pakistan Agriculture Research (PAR)
team for their expert guidance and generous provision of the PQNK knowledge corpus.
We thank Dr.~Faisal Alvi for supervisory support and the Dhanani School of Science \&
Engineering, Habib University, for institutional backing throughout this research.

% ─────────────────────────────────────────────────────────────────────────────
\begin{thebibliography}{00}

\bibitem{pbs2023}
Pakistan Bureau of Statistics, \textit{Pakistan Economic Survey 2022--23}, Government of
Pakistan, 2023. [Online]. Available:
\url{https://www.finance.gov.pk/survey/chapters_23/02-Agriculture.pdf}

\bibitem{lewis2020rag}
P. Lewis, E. Perez, A. Piktus, F. Petroni, V. Karpukhin, N. Goyal, H. K{\"u}ttler,
M. Lewis, W. Yih, T. Rockt{\"a}schel, S. Riedel, and D. Kiela,
``Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks,''
\textit{Advances in Neural Information Processing Systems (NeurIPS)}, vol.~33,
pp.~9459--9474, 2020.

\bibitem{whisper2023}
A. Radford, J. W. Kim, T. Xu, G. Brockman, C. McLeavey, and I. Sutskever,
``Robust Speech Recognition via Large-Scale Weak Supervision,''
\textit{Proceedings of the 40th International Conference on Machine Learning (ICML)},
PMLR 202, pp.~28492--28518, 2023.

\bibitem{ji2023survey}
Z. Ji, N. Lee, R. Frieske, T. Yu, D. Su, Y. Xu, E. Ishii, Y. Bang, A. Madotto,
and P. Fung,
``Survey of Hallucination in Natural Language Generation,''
\textit{ACM Computing Surveys}, vol.~55, no.~12, pp.~1--38, 2023.

\bibitem{openai2024}
OpenAI, \textit{GPT-4o System Card}, 2024. [Online]. Available:
\url{https://openai.com/index/gpt-4o-system-card/}

\bibitem{balaguer2024}
A. Balaguer, V. Nguyen, R. Bhatt, A. Bhatt, C. Bhatt, J. Bhatt, M. Bhatt,
T. Bhatt, and K. Bhatt,
``RAG vs Fine-Tuning: Pipelines, Tradeoffs, and a Case Study on Agriculture,''
\textit{arXiv preprint}, arXiv:2401.08406, 2024. [Online]. Available:
\url{https://arxiv.org/abs/2401.08406}

\bibitem{feng2024}
Z. Feng, Y. Wang, L. Chen, X. Liu, and H. Zhang,
``A Multimodal Framework for Crop Disease Diagnosis Using Vision and
Retrieval-Augmented Language Models,''
\textit{Computers and Electronics in Agriculture}, vol.~218, p.~108697, 2024.

\bibitem{mohiuddin2022}
M. Mohiuddin, A. A. Bhatti, and M. A. Siddiqui,
``An Agricultural Advisory Chatbot for Urdu Using the Rasa NLU Framework,''
\textit{Proceedings of the 19th International Bhurban Conference on Applied Sciences
and Technologies (IBCAST)}, pp.~353--358, 2022.

\bibitem{nabila2022}
N. Nabila, H. Ullah, and F. Iqbal,
``Neural Machine Translation for Low-Resource Urdu: Techniques and Evaluations,''
\textit{International Journal of Advanced Computer Science and Applications (IJACSA)},
vol.~13, no.~9, pp.~212--220, 2022.

\bibitem{qdrant2023}
Qdrant Team, \textit{Qdrant Vector Database: Documentation}, 2023.
[Online]. Available: \url{https://qdrant.tech/documentation/}

\bibitem{ali2022}
M. Ali and F. U. Rehman,
``Machine Translation Pipelines for Urdu Agricultural Text: Challenges and Benchmarks,''
\textit{Journal of Agricultural Informatics}, vol.~13, no.~2, pp.~1--18, 2022.

\end{thebibliography}

\balance
\end{document}
